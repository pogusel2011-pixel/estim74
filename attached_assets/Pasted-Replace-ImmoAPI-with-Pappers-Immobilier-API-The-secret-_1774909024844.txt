Replace ImmoAPI with Pappers Immobilier API. 
The secret key is named PAPPERS_API_KEY.

Base URL: https://api-immobilier.pappers.fr/v1/
Auth header: api-key: YOUR_KEY

Please implement the following integrations:

1. DPE AUTO-FETCH (most important):
In the property form, when an address is entered 
and geocoded, automatically call:
GET /parcelles?adresse=[address]&bases=dpe
to retrieve the real DPE of the property.
Pre-fill the DPE field automatically if found.
Show "DPE récupéré automatiquement" badge.

2. PROPERTY DETAILS AUTO-FETCH:
When address is entered, also fetch:
GET /parcelles?adresse=[address]&bases=batiments,ventes
to pre-fill:
- yearBuilt (from batiments.annee_construction)
- Recent sales on same parcel (for context)

3. COMPARABLE SALES via Pappers:
Create lib/pappers/client.ts with function 
fetchPappersSales(lat, lng, radiusM, typeLocal):
GET /parcelles?latitude=[lat]&longitude=[lng]
&distance=[radius]&bases=ventes
&type_local_vente=[typeLocal]
&date_vente_min=[24 months ago]
Use as additional data source alongside CSV DVF.

4. Update lib/dvf/client.ts:
Remove ImmoAPI fallback (IMMOAPI_KEY)
Add Pappers as fallback when CSV has < 5 results

5. Add PAPPERS_API_KEY to environment variables

MOTEURIMMO / ACTIVE LISTINGS:
6. Verify lib/moteurimmo/client.ts uses correctly:
- Secret: MOTEURIMMO_API_KEY
- Base URL: https://api.moteurimmo.fr/v1
- Endpoint: GET /annonces
- Parameters: type_bien, code_postal, 
  surface_min, surface_max, nb_pieces_min, 
  nb_pieces_max, nb_resultats (max 20)
- Header: X-Api-Key: [MOTEURIMMO_API_KEY]
7. If MOTEURIMMO_API_KEY is missing or API returns 
   error, fail silently and show 
   "Annonces non disponibles" in the listings panel
8. Test the listings panel on a new estimation 
   in Annecy (74000) and confirm active listings 
   are returned or show the correct error message

Important: Keep the local CSV as primary source.
Pappers is the new fallback and enrichment source.
Moteurimmo remains the active listings source.